1. Fix iframe inject to have a subitem Add/Remove to whitelist
2. Transmit data
3. Store data
4. Treat data (if userId is in dictionnary then set mode to 0 (blacklist) else 1 (whitelist))
5. Implement whitelist check while doing XHR interception
6. Testing
7. Icons
8. Description extension + documentation
9. Deploy (By 20th december)
